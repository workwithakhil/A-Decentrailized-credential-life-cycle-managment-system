# Copy to .env and fill in. NEVER commit the real .env
# Use a throwaway wallet that holds only testnet funds.
PRIVATE_KEY=
AMOY_RPC_URL=https://rpc-amoy.polygon.technology
POLYGONSCAN_API_KEY=
REPORT_GAS=false
